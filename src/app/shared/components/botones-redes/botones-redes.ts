import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-botones-redes',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './botones-redes.html',
  styleUrl: './botones-redes.scss',
})
export class BotonesRedes {}
